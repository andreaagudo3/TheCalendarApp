import Foundation
import UIKit

struct ViewConstants {
    static let cornerRadius: CGFloat = 8
}

struct DateFormats {
    static let databaseFormat = "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'"
    
    struct Calendar {
        static let calendarTitleFormat = "MMMM, yyyy"
    }
}
